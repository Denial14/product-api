app:
  name: product-api
  env: development
  port: 8080

database:
  name: mydb
  host: localhost
  port: 5432
  max_connections: 25
  max_idle_connections: 10
  connection_timeout: 30s

logging:
  level: info
  format: json
  output: stdout